<h1>商品一覧</h1>
<p>全<%= @items.total_count %>件</p>
<% @items.each do |item| %>
  <div><%=image_tag item.get_image(300,200) %></div>
  <p><%= item.name %></p>
  <p>¥<%= item.with_tax_price %></p>
<% end %>
<%= paginate @items %>